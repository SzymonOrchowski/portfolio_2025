import React from 'react';
import ThemeToggle from '@/components/common/ThemeToggle';

const Navbar = () => {
  return (
    <nav>
      <p>Navbar</p>
      <ThemeToggle />
    </nav>
  );
};

export default Navbar;
